const Schedule = () => {
    //here will be a calendar to where study tasks will be added automatically if there is a deadline or such an option to add in calendar
    //so the user can understand their schedule
};

export default Schedule;
